const square = function(x) {
    return x * x; 
};

console.log(square(9)); //function with param

const makeNoise = function() {
    return "WOWOOWOWO"; 
}

console.log(makeNoise()); //function with no params

const halve = function(n) {
    return n / 2; 
}

console.log(halve(9));